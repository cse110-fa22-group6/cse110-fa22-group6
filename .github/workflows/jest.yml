name: Jest
on:
  push:
    branches:
      - "**"
  pull_request:
    branches:
      - "**"
steps:
  - uses: actions/checkout@v3

  - name: Install packages
    run: npm install

  - name: Run tests
    run: npm test
