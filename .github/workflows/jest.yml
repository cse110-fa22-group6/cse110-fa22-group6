name: Jest
on:
  push:
    branches:
      - "**"
  pull_request:
    branches:
      - "**"
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: puppeteer-chrome
        uses: tj-actions/puppeteer@v3.2
